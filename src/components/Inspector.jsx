import React from 'react'

export function Inspector({ page, section, project, onPageChange, onThemeChange, onSectionChange, onSaveBlock, onRemoveSection }) {
  return <aside className="sidebar right-sidebar"><div className="panel-block"><h3>Inspector</h3><div className="inspector-form"><label>Page name<input value={page?.title || ''} onChange={(event) => onPageChange('title', event.target.value)} /></label><label>Slug<input value={page?.slug || ''} onChange={(event) => onPageChange('slug', event.target.value)} /></label><label>SEO title<input value={page?.seoTitle || ''} onChange={(event) => onPageChange('seoTitle', event.target.value)} /></label><label>SEO description<textarea rows="3" value={page?.seoDescription || ''} onChange={(event) => onPageChange('seoDescription', event.target.value)} /></label><label>Status<select value={page?.status || 'draft'} onChange={(event) => onPageChange('status', event.target.value)}><option value="draft">Draft</option><option value="published">Published</option></select></label><label>Accent<input type="color" value={project.theme.accent} onChange={(event) => onThemeChange('accent', event.target.value)} /></label>{section && <><label>Section title<input value={section.title || ''} onChange={(event) => onSectionChange({ title: event.target.value })} /></label>{section.type === 'text' && <label>Body<textarea rows="4" value={section.body || ''} onChange={(event) => onSectionChange({ body: event.target.value })} /></label>}<button className="secondary-btn" onClick={onSaveBlock}>Save as block</button><button className="danger-btn" onClick={onRemoveSection}>Remove section</button></>}</div></div><div className="panel-block"><h3>Project JSON</h3><pre className="code-block">{JSON.stringify(project, null, 2)}</pre></div></aside>
}
